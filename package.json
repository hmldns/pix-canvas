{
  "name": "infinite-pixel-canvas",
  "version": "1.0.0",
  "description": "A real-time collaborative pixel canvas",
  "private": true,
  "scripts": {
    "build": "make build",
    "build:libs": "cd libs/common-types && npm run build && cd ../utils && npm run build",
    "dev": "make up",
    "dev-backend": "npm run build:libs && cd apps/backend && npm run dev",
    "test": "make test-backend"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}